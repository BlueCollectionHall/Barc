# Barc 蔚蓝收录馆 - 功能文档

> 本文档面向 AI 开发者编写，用于描述系统功能。系统目前为手写代码，未来将由 AI 接手维护和开发。

---

## 一、系统概述

**蔚蓝收录馆 (Barc)** 是一个二次元学园/部团/学生/作品管理系统，主要用于管理二次元角色（学生）及其创作（作品）。

### 核心定位
- 收录二次元学园（School）、部团（Club）、学生（Student）的信息
- 管理学生上传的作品（Work），支持分类、评论、认领等功能
- 提供用户注册登录、消息留言、反馈建议等服务

---

## 二、技术栈与部署

### 后端 BarcBackend
| 类别 | 技术 |
|------|------|
| 框架 | Spring Boot 3.4.5 |
| Java 版本 | 17 |
| 数据库 | MySQL |
| ORM | MyBatis (MyBatis Spring Boot Starter 3.0.4) |
| 数据库连接池 | Druid |
| 认证 | JWT (jjwt 0.12.6) |
| 文件存储 | MinIO + 腾讯云 COS |
| 邮件服务 | Spring Boot Mail |
| WebSocket | Spring WebSocket |
| 富文本解析 | Apache Tika |
| 工具库 | Lombok, Commons-IO |

#### 后端部署信息
| 项目 | 值 |
|------|-----|
| 服务端口 | 51000 |
| 数据库地址 | jdbc:mysql://miaoyu.asia:51001/ba |
| 数据库用户名 | ba |
| 邮件服务 | naigos.cn (SSL 465) |
| 邮件账号 | barc@naigos.cn |

### 前端 BarcFrontend
| 类别 | 技术 |
|------|------|
| 框架 | Vue 3 + TypeScript |
| 构建工具 | Vite |
| UI 库 | Ant Design Vue 4.x + Element Plus 2.x |
| 状态管理 | Pinia |
| 路由 | Vue Router 4 |
| HTTP 客户端 | Axios |
| 富文本编辑器 | VueQuill |
| 图片裁剪 | Cropper.js |
| 图标 | Ant Design Icons |

---

## 三、功能模块

### 3.1 用户模块 (User)

#### 3.1.1 用户基础信息 (user_basic)
| 字段 | 类型 | 说明 |
|------|------|------|
| uuid | String | 主键，用户唯一标识 |
| username | String | 用户名（唯一） |
| password | String | SHA256 加密密码 |
| password_version | int | 密码加密版本 |
| email | String | 邮箱（唯一） |
| email_verified | boolean | 邮箱是否已验证 |
| telephone | String | 手机号（可空） |
| safe_level | int | 安全等级 |
| created_at | Date | 创建时间 |
| updated_at | Date | 更新时间 |

#### 3.1.2 用户档案信息 (user_archive)
| 字段 | 类型 | 说明 |
|------|------|------|
| uuid | String | 外键，关联 user_basic |
| nickname | String | 昵称 |
| avatar | String | 头像 URL |
| gender | int | 性别 |
| birthday | Date | 生日 |
| age | int | 年龄 |
| permission | int | 权限等级 |
| updated_at | Date | 更新时间 |

#### 3.1.3 注册与登录
| 功能 | 说明 |
|------|------|
| 邮箱注册 | 需要邮箱验证码 |
| 用户名登录 | 用户名 + 密码 |
| 邮箱登录 | 邮箱 + 密码 |
| 奶果账号登录 | 通过第三方服务(Naigos)登录，自动创建本地账号 |

#### 3.1.4 验证码 (verification_code)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 唯一ID |
| code | String | 6位验证码 |
| username | String | 关联用户名 |
| scenario | String | 用途场景 |
| create_at | Date | 创建时间 |
| expiration_at | Date | 过期时间 |
| used | boolean | 是否已使用 |

---

### 3.2 学园模块 (School)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键，唯一ID |
| cn_name | String | 中文名（唯一，必填） |
| jp_name | String | 日文名 |
| kr_name | String | 韩文名 |
| en_name | String | 英文名 |
| introduce | String | 介绍文本 |
| logo | String | Logo URL |
| beautify_logo | String | 美化 Logo |
| bg | String | 背景图 |

#### API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/schools_all | 获取全部学园 |
| GET | /api/school_only | 获取单个学园 |
| POST | /api/up_school | 上传/修改学园 |

---

### 3.3 部团模块 (Club)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键，唯一ID |
| school | String | 所属学园 ID |
| cn_name | String | 中文名（唯一，必填） |
| jp_name | String | 日文名 |
| kr_name | String | 韩文名 |
| en_name | String | 英文名 |
| logo | String | Logo URL |
| bg | String | 背景图 |

#### API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/clubs_all | 获取全部部团 |
| GET | /api/clubs_by_school | 通过学园 ID 获取部团 |
| GET | /api/club_only | 获取单个部团 |
| POST | /api/up_club | 上传/修改部团 |

---

### 3.4 学生模块 (Student)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键，唯一ID（自动生成） |
| cn_name | String | 中文名（唯一，必填） |
| jp_name | String | 日文名 |
| kr_name | String | 韩文名 |
| en_name | String | 英文名（唯一，必填） |
| introduce | String | 介绍文本 |
| avatar_square | String | 方形头像 |
| avatar_rectangle | String | 竖长方形头像（必填） |
| body_image | String | 立绘图（必填） |
| school | String | 所属学园 ID |
| club | String | 所属部团 ID（必填） |

#### API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/students_all | 获取全部学生 |
| GET | /api/students_by_school | 通过学园获取学生 |
| GET | /api/students_by_club | 通过部团获取学生 |
| GET | /api/student_only | 获取单个学生 |
| POST | /api/up_student | 上传/修改学生 |

---

### 3.5 分类模块 (Category)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键，唯一ID |
| parent_id | String | 父级分类 ID（顶级为 null） |
| name | String | 分类名（唯一） |
| level | int | 层级 |
| sort | int | 排序顺序 |
| is_enabled | boolean | 是否启用 |
| icon | String | 图标 |

#### 特点
- 支持无限层级分类
- 可通过任意层级获取该节点及所有后代

#### API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/categories_all | 获取全部分类 |
| GET | /api/categories_by_category | 通过分类 ID 获取本级及后代 |

---

### 3.6 作品模块 (Work)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键，唯一ID（自动生成） |
| title | String | 标题（必填） |
| description | String | 简介 |
| content | String | 富文本内容 |
| banner_image | String | Banner 图（必填） |
| cover_image | String | 封面图（必填） |
| view_count | int | 浏览量 |
| like_count | int | 点赞量 |
| author | String | 上传者 UUID |
| author_nickname | String | 上传者昵称 |
| uploader | String | 上传者 UUID |
| is_claim | boolean | 是否已被认领 |
| status | int | 状态：1正常 2封禁 3下架 |
| student | String | 归属学生 ID（必填） |
| created_at | Date | 创建时间（ISO8601） |
| updated_at | Date | 更新时间（ISO8601） |

#### 关联关系
- 作品与分类：多对多 (work_category)
- 作品与学生：多对一
- 作品与用户：多对一

#### API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/works_all | 获取全部作品 |
| GET | /api/works_by_category | 通过分类获取 |
| GET | /api/works_by_school | 通过学园获取 |
| GET | /api/works_by_club | 通过部团获取 |
| GET | /api/works_by_student | 通过学生获取 |
| GET | /api/work_only | 获取单个作品 |

#### 作品评论 (work_comment)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| work_id | String | 作品 ID |
| author | String | 评论者 UUID |
| content | String | 评论内容 |
| created_at | Date | 创建时间 |

#### 作品评论回复 (work_comment_reply)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| parent_id | String | 父评论 ID |
| author | String | 回复者 UUID |
| reply_user | String | 被回复者 UUID |
| content | String | 回复内容 |
| created_at | Date | 创建时间 |

#### 作品认领 (work_claim)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| work_id | String | 作品 ID |
| applicant_uuid | String | 申请人 UUID |
| created_at | Date | 申请时间 |

---

### 3.7 消息留言模块 (MessageBoard)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| author | String | 留言者 UUID |
| content | String | 留言内容 |
| created_at | Date | 创建时间 |

---

### 3.8 反馈模块 (Feedback)

#### 3.8.1 反馈类型 (feedback_type)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| name | String | 类型名称 |

#### 3.8.2 反馈选项 (feedback_option)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| name | String | 选项名称 |
| parent_id | String | 关联反馈类型 |

#### 3.8.3 作品举报 (feedback_work)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| work_id | String | 被举报作品 |
| ipv4 | String | 举报者 IP |
| device_info | String | 设备信息 |
| author | String | 举报者 UUID |
| reason_option | String | 举报原因选项 |
| content | String | 详细描述 |
| email | String | 联系邮箱 |
| status | boolean | 处理状态 |
| note | String | 管理员备注 |

#### 3.8.4 通用反馈 (feedback)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| target_id | String | 目标对象 ID |
| ipv4 | String | 反馈者 IPv4 |
| ipv6 | String | 反馈者 IPv6 |
| author | String | 反馈者 UUID |
| email | String | 联系邮箱 |
| content | String | 反馈内容 |
| echo | String | 管理员回复 |
| type | String | 反馈类型 |
| status | enum | 状态：PENDING/PROCESSING/COMPLETED/REJECTED |
| created_at | Date | 创建时间 |
| updated_at | Date | 更新时间 |

---

### 3.9 公告模块 (Notice)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| title | String | 标题 |
| content | String | 内容 |
| author | String | 发布者 UUID |
| created_at | Date | 创建时间 |
| updated_at | Date | 更新时间 |

---

### 3.10 动态/趋势模块 (Trend)

#### 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| title | String | 标题 |
| content | String | 内容 |
| author | String | 发布者 UUID |
| created_at | Date | 创建时间 |
| updated_at | Date | 更新时间 |

#### 动态图片 (trend_image)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| trend_id | String | 关联动态 ID |
| sort | int | 排序 |
| image_url | String | 图片 URL |
| image_name | String | 图片名称 |
| created_at | Date | 创建时间 |

---

### 3.11 背景图模块 (Background)

管理端支持为各页面（登录/注册、首页、加入我们、意见反馈、留言板、学生详情）上传/启用/排序背景图。

#### 页面背景字段
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| module | String | 所属模块 code（sign/home/join/feedback/messageboard/student） |
| object_key | String | COS 对象 key |
| filename | String | 文件名 |
| sort_order | int | 排序 |
| enabled | boolean | 是否启用 |
| time_period | String | 时段标签：`day`/`eventing`/`night`，NULL 表示任意时段 |
| festival | String | 节日标签：`newyear`，NULL 表示非节日通用图 |
| created_by | String | 创建人 UUID |
| created_at / updated_at / deleted_at | Date | 创建/更新/删除时间 |

#### 公开接口
- `GET /api/background/all`（无需登录）：返回所有启用背景图，按 `module` 分组，每项为 `{ url, time_period, festival }`（url 已解析为 COS 签名 URL）。

#### 首页背景图「按时间 + 节日」切换逻辑（用户端）
首页背景图（`HomeBgComp.vue`）从启用图池中按当前场景选一张（每次访问随机换一张，不自动轮播）：
1. **节日**：11 月 - 次年 2 月视为新年前后，优先取 `festival=newyear` 的图；无则用非节日通用图。非节日季只用 `festival=NULL` 的图。
2. **时段**：在选定池内优先取匹配当前时段的图（白天 6-17 点、傍晚 17-19 点、夜晚 0-6 与 19-24 点），含未标注时段的通用图；无匹配则退回整个池。
3. **兜底**：管理端无任何启用图时，回退到写死的时段/新年图片。

其它模块（sign/join/feedback/messageboard）仅从该模块启用图中随机取一张。

---

## 四、前端页面结构

| 路由 | 页面 | 说明 |
|------|------|------|
| / | HomeView | 首页 |
| /sign | SignView | 注册登录页 |
| /student | StudentView | 学生详情页 |
| /work | WorkView | 作品页（含搜索、筛选、详情） |
| /account | AccountView | 用户账号页 |
| /manage | ManageView | 管理页 |
| /upload | UploadView | 上传作品页 |
| /update | UpdateView | 更新资料页 |
| /feedback | FeedbackView | 反馈页 |
| /message | MessageBoardView | 留言板页 |
| /notice | NoticeView | 公告页 |
| /join | JoinView | 加入我们页 |
| /service | ServiceView | 服务条款页 |
| /private | PrivateView | 隐私政策页 |
| /category | CategoryView | 分类页 |

---

## 五、认证与权限

### JWT 认证
- Token 存储在请求头 `Authorization: Token <token>`
- 登录成功返回 Token
- 修改操作需要携带 Token

### 身份枚举 (UserIdentityEnum)

| 枚举值 | 中文名 | 说明 |
|--------|--------|------|
| `USER` | 用户 | 普通用户身份 |
| `MANAGER` | 管理者 | 管理员身份 |

### 权限系统（位运算）

系统采用**位运算**实现权限控制，通过 `user_archive.identity` 和 `user_archive.permission` 字段组合实现细粒度权限管理。

#### 普通用户权限 (identity = USER)

| 常量 | 十进制值 | 二进制 | 说明 |
|------|---------|--------|------|
| `USER` | 1 | 0000 0001 | 会员：浏览、留言、点赞、收藏、反馈 |
| `UPPER` | 2 | 0000 0010 | 收录员：发布、收录、处置作品 |
| `CREATOR` | 4 | 0000 0100 | 创作者：与收录员相同 |

#### 管理者权限 (identity = MANAGER)

| 常量 | 十进制值 | 二进制 | 说明 |
|------|---------|--------|------|
| `DISCIPLINARY_COMMITTEE` | 1 | 0000 0001 | 风纪委员：警告、公告、审批认领 |
| `FIR_MAINTAINER` | 2 | 0000 0010 | 一级管理员：处理评论/留言举报 |
| `SEC_MAINTAINER` | 4 | 0000 0100 | 二级管理员：处理作品举报 |
| `THI_MAINTAINER` | 8 | 0000 1000 | 三级管理员：处理用户举报 |
| `ADMINISTRATOR` | 16 | 0001 0000 | 副馆长：管理所有非馆长用户 |
| `ADVANCED_ADMINISTRATOR` | 32 | 0010 0000 | 馆长：超级管理员，管理全部 |

#### 权限对比工具 (ComparePermission)

```java
// 比较 basic >= target（权限值是否大于等于目标）
compare(int your, int target)

// 判断是否有某权限 (your & target) != 0
has(int your, int target)
```

#### 鉴权注解 (@RequireUserAndPermissionAnno)

```java
@RequireUserAndPermissionAnno({
    @RequireUserAndPermissionAnno.Check(
        identity = UserIdentityEnum.MANAGER,      // 身份要求
        targetPermission = PermissionConst.THI_MAINTAINER,  // 目标权限
        isSuchElseRequire = false                // 是否需要满足全部条件
    ),
    @RequireUserAndPermissionAnno.Check(uuidIndex = 1)  // UUID 参数位置
})
```

#### 管理端功能权限对照

| 管理功能 | 所需权限 |
|----------|----------|
| 学园/部团/学生管理 | `FIR_MAINTAINER` (2) |
| 作品审核/管理 | `SEC_MAINTAINER` (4) |
| 用户管理 | `THI_MAINTAINER` (8) |
| 管理员管理 | `ADMINISTRATOR` (16) |
| 全部权限 | `ADVANCED_ADMINISTRATOR` (32) |

---

## 六、存储策略

### 文件存储
- **腾讯云 COS**: 统一使用腾讯云 COS 对象存储
- **存储方式**: 私有桶 + 签名 URL
- **访问模式**: 通过后端生成临时签名 URL 访问，签名有效期有限

#### 签名 URL 流程
```
1. 前端请求后端上传文件
2. 后端上传文件到 COS 私有桶
3. 前端需要访问时，后端生成带签名的临时 URL
4. 前端通过签名 URL 访问文件
```

### 文件类型
- 用户头像
- 学生立绘/头像
- 作品图片
- 作品 Banner/封面
- 动态图片

---

## 七、业务规则

### 7.1 上传类型 (up_type)
| 值 | 说明 |
|------|------|
| upload | 新增 |
| update | 更新 |

### 7.2 作品状态 (status)
| 值 | 说明 |
|------|------|
| 1 | 正常 |
| 2 | 封禁 |
| 3 | 下架 |

### 7.3 多语言支持
- 学园、部团、学生均支持：
  - 中文名 (cn_name)
  - 日文名 (jp_name)
  - 韩文名 (kr_name)
  - 英文名 (en_name)

### 7.4 第三方登录
- 奶果 Naigos 登录：
  - 自动创建本地账号
  - 初始密码为 123456
  - 若无邮箱，使用 QQ 号 @qq.com

---

## 八、数据库表清单

| 表名 | 说明 |
|------|------|
| user_basic | 用户基础信息 |
| user_archive | 用户档案信息 |
| verification_code | 验证码 |
| school | 学园 |
| club | 部团 |
| school_club | 学园-部团关联 |
| student | 学生 |
| category | 分类 |
| work | 作品 |
| work_category | 作品-分类关联 |
| work_comment | 作品评论 |
| work_comment_reply | 评论回复 |
| work_claim | 作品认领 |
| work_image | 作品图片 |
| work_cover_image | 作品封面 |
| message_board | 留言板 |
| feedback_type | 反馈类型 |
| feedback_option | 反馈选项 |
| feedback_work | 作品举报 |
| feedback | 通用反馈 |
| trend | 动态 |
| trend_image | 动态图片 |
| notice | 公告 |
| barc_naigos_uuid | 奶果账号关联 |

---

## 九、注意事项（面向 AI 开发）

1. **ID 生成策略**：部分实体 ID 由后端自动生成（使用 UUID 或雪花算法），前端不应自增
2. **时间格式**：统一使用 ISO8601 格式
3. **多对多关系**：作品与分类通过中间表关联
4. **软删除考虑**：部分表使用外键级联删除，需注意数据一致性
5. **安全等级**：邮箱验证、手机绑定等会影响 safe_level
6. **文件上传**：需关注存储服务的可用性和配额
7. **评论嵌套**：仅支持一层回复（parent_id）
8. **分类树**：支持无限层级，但需注意前端渲染性能
